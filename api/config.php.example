<?php
// Шаблон конфига для api/lead.php.
// На сервере: cp api/config.php.example api/config.php и заполнить значения.
// api/config.php в .gitignore — в репо не коммитится.

// ⚠️ Бот заявок ОБЩИЙ у трёх клиник (владелец, 13.08.2026) — тот же, что у
// «Ангел-Дент» и «Версаля». Разные у них только группы: у «Венеции»
// chat_id -5530724524. Токен уже лежит на сервере в конфиге Ангела, новый
// брать у @BotFather не нужно — достаточно скопировать (см. CLAUDE.md,
// раздел про заявки). Как запасной путь деплой умеет писать этот файл из
// секрета репозитория VENECIA_TG_TOKEN.
define('TELEGRAM_BOT_TOKEN', 'PUT-YOUR-BOT-TOKEN-HERE');
define('TELEGRAM_CHAT_ID',  '-5530724524');
