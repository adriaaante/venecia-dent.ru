# Венеция (venecia-dent.ru) — заметки для агента

Семейная стоматология «Венеция», **Мытищи, ул. Мира, д. 37** (индекс 141008,
координаты 55.918478, 37.721126 — уже в JSON-LD и карте). Третья клиника
владельца (после Angel-Dent и Versal-Dent), архитектура сайта та же:
плоская статика без сборщика. **Массовая правка хедера/футера/FAB =
трогаем каждый HTML** (или Python-патч).

Позиционирование: премиально по оформлению, но слово «премиум» в текстах
НЕ используем. Цены и услуги = Angel-Dent (сверено 2026-07-20), НО:
⚠️ **в «Венеции» НЕТ КТ и НЕТ детского кабинета** (решение владельца,
2026-07-24) — никакой томографии, акции «КТ + план» и детской
стоматологии нигде не упоминать. Вместо детской в услугах — «Отбеливание
зубов» (otbelivanie). Тексты написаны с нуля (не дубли Angel/Versal).

## ⚠️ Открытые TODO (сайт собран до запуска клиники)

- ~~Телефон/email~~ — СДЕЛАНО 31.07.2026: реальные контакты клиники
  `+7 (916) 838-08-88` / `venecia.1@mail.ru` (плюс `wa.me/79168380888`,
  `t.me/+79168380888`) проставлены во всех HTML, main.js и JSON-LD.
  Менять — только через `python3 scripts/set-contacts.py --phone "…" [--email …]`.
  ⚠️ Скрипт раньше обходил лишь корень и `services/` и **молча пропустил
  `doctors/`** — страницы врачей остались с заглушкой. Исправлено: теперь
  обход рекурсивный (`**/*.html`), любые новые разделы подхватятся сами.
- **Яндекс.Метрика** — placeholder `00000000` в `<head>` каждой страницы
  + `YM_COUNTER_ID = 0` в `assets/js/main.js`. Заменить, когда владелец
  пришлёт id (тем же set-contacts.py, флаг `--metrika ID`).
- **Юрлицо/лицензия** — **владелец подтвердил 10.08.2026: Венеция работает
  под тем же ООО «АНГЕЛ-ДЕНТ»** (ИНН 5012077543, ОГРН 1135012000936,
  лицензия ЛО41-01162-50/00299266 от 19.07.2016) — реквизиты уже вписаны в
  наружку (`_materials/naruzhka/`). На `legal.html`/`privacy.html`/футере
  пока `<!-- TODO -->`-заглушки — заполнить по этим реквизитам при
  следующей правке. ⚠️ Медлицензия привязана к адресам мест деятельности:
  проверить/напомнить владельцу, что адрес Мытищи, ул. Мира, 37 внесён в
  лицензию (переоформление в Росздравнадзоре), иначе риск при проверке.
- ~~Врачей нет~~ — СДЕЛАНО 2026-07-28: раздел `doctors/` (index + 3
  страницы: `drobkova`, `kilasoniya`, `kendabaeva`), секция на главной,
  пункт «Врачи» в меню и футере всех страниц, sitemap. Детали — раздел
  «Врачи и портфолио» ниже. Открыто: **фото Киласонии** (placeholder.svg),
  `<!-- TODO -->` в образовании/сертификатах врачей.
- ~~Фото клиники~~ — СДЕЛАНО 2026-07-21: реальные фото в
  `assets/img/clinic/` (hero, about, галерея 6 шт., og-banner) из папки
  Drive владельца `1q77x0l2jd7PzNyGXqD6YiDTutUxuaTxf` (там 14 исходников).
  Сгенерированные Higgsfield остались только на карточках услуг
  (`assets/img/services/*.webp`).
- **Отзывов нет** — честные заглушки на index/reviews. Рейтинги и
  AggregateRating НЕ добавлять, пока нет реальной карточки Яндекса.
- **Telegram-бот заявок** — создать бота и группу, на сервере
  `cp api/config.php.example api/config.php` + токен/chat_id.

## Данные клиники (источник правды)

- Адрес: Московская область, г. Мытищи, ул. Мира, д. 37, 141008
- График: ежедневно 10:00–20:00
- Домен: venecia-dent.ru
- Телефон: **+7 (916) 838-08-88** (`tel:+79168380888`, `wa.me/79168380888`,
  `t.me/+79168380888`)
- E-mail: **venecia.1@mail.ru**
- Меняются контакты → `scripts/set-contacts.py`, руками не искать по файлам.

## Фирменный стиль

Полная палитра/типографика — **`BRAND.md`**. Коротко: лагуна `#0F6E66`,
терракота `#C75B39` (CTA), алебастр `#F7FAF8`, чернила `#13292A`.
Шрифты: **Prata** (заголовки) + **Onest** (текст), Google Fonts.
Фирменный мотив — **арка** (маски фото `--radius-arch`, мини-арки иконок)
и терракотовый ромб (буллеты, eyebrow). Токены — в `:root` styles.css.
НЕ использовать: золото (это Versal), голубой (это Angel).

Логотип: `assets/img/logo.svg` — исходник (зуб с окном-аркой на плитке
лагуны). PNG/фавиконки перегенерировать: `python3 scripts/build-favicons.py`
(нужны `pip install cairosvg pillow`).

## Структура

Корень: `index.html`, `about.html`, `promotions.html`, `reviews.html`,
`contacts.html`, `ceny.html` (полный прайс), `garantii.html`,
`pervyj-vizit.html` (маршрут пациента), правовые (`legal`, `privacy`,
`consent`, `oferta`, `cookies`), `404.html`.
`services/` — `index.html` + 9 услуг: implantaciya, ortodontiya,
terapiya, hirurgiya, **otbelivanie** (уникальный slug — вместо detskaya),
protezirovanie, viniry, gigiena, parodontologiya.
`doctors/` — `index.html` + 3 врача: `drobkova` (Дробкова Кристина
Олеговна, ортодонт), `kilasoniya` (Киласония Шорена Гиулиевна, хирург,
терапевт), `kendabaeva` (Кендабаева Зухро Бурхоновна, гигиенист).

Навигация: Главная · Услуги · Цены · Врачи · Акции · Первый визит ·
О клинике · Контакты. «Гарантии» и «Отзывы» — в футере.

Шаблон услуги: page-hero → usps (4 факта) → «Как мы работаем» (текст +
фото) → цены → этапы (4 шага) → FAQ → перелинковка (Цены/Гарантии/Первый
визит/Акции) → смежные услуги → CTA. JSON-LD: MedicalProcedure + FAQPage +
BreadcrumbList на каждой; Dentist (`@id` `…/#clinic`) — на index, about,
contacts.

Сайт собирался Python-генератором (одноразовый, в истории сессии);
дальше правки — прямо в HTML или свежим скриптом в `scripts/`.

## Шапка: три яруса по ширине (правлено 31.07.2026)

Меню Венеции **длиннее**, чем у Angel/Версаля: 8 пунктов, среди них
«Первый визит» и «О клинике», плюс пилюли с внутренними отступами.
Содержимое шапки — ~1246px против ~1070px у соседних клиник, а
контентный контейнер всего 1180px. Поэтому шапка (и топбар — чтобы
края совпадали) имеют собственный `max-width: 1400px`, и заведены три
яруса:

| Ширина окна | Что показываем |
|---|---|
| ≤ 1279px | бургер (меню скрыто, телефон скрыт) |
| 1280–1420px | полное меню в уплотнённом виде (`.nav a` 13.6px/7px) |
| ≥ 1421px | полное меню в просторном виде |

⚠️ Порог бургера **1279px, а не 1080px**: на 1081–1279 кнопка
«Записаться» уезжала за правый край экрана (проверено в Chromium,
переполнение до 208px). Добавляешь пункт в меню — **перемеряй**, иначе
кнопка снова уедет; ярусы держать в этом же блоке `styles.css`.

⚠️ `.nav + .header__cta { margin-left: 0 }` — правило для видимого
меню (вправо блок толкает `.nav` своим `margin-left:auto`). Там, где
меню скрыто, прижатие надо возвращать явно (`margin-left: auto`), иначе
кнопка и бургер прилипают к логотипу.

⚠️ **Инлайновый `style="grid-template-columns:…"` в HTML сильнее
медиазапросов** — сетка не схлопнется на телефоне и разъедет страницу
вширь. Так было на `pervyj-vizit.html`. Нужна другая раскладка — брать
класс (`.steps--4`), а не инлайн-стиль.

## Цены: неразрывный пробел обязателен

В прайсе у последней колонки на ≤640px стоит `white-space: normal` (иначе
длинное «от 180 000 ₽ за челюсть» распирает таблицу на узких экранах).
`normal` разрешает перенос по **любому** пробелу — включая пробел внутри
числа, и «бесплатно 1 000 ₽» рвалось как «бесплатно 1» / «000 ₽».

Поэтому разряды и знак рубля разделяются **неразрывным** пробелом (U+00A0).
Правишь цены — прогони после этого:

```
python3 scripts/fix-price-nbsp.py          # проставить (идемпотентно)
python3 scripts/fix-price-nbsp.py --check  # только проверить, код 1 если надо править
```

⚠️ Предлог «от» к сумме **не приклеивать**: неделимый кусок «от 180 000 ₽»
не помещается, и таблица «Протезирование» вылезает за общий край на 320px.
На ≤380px у ячеек прайса уменьшены отступы — иначе неразрывное
«от 28 000 ₽» опять не влезает.

У Angel/Версаля этой болячки нет: там колонка цен остаётся `nowrap` на всех
ширинах, числа физически не рвутся.

## Проверка вёрстки в браузере

Часть багов видна только на конкретной ширине и не ловится глазами:

```
python3 scripts/check-layout.py                 # все вьюпорты 320–1600
python3 scripts/check-layout.py --vw 320,1280   # только нужные
python3 scripts/check-layout.py --root ../Angel-Dent-site
```

Ловит: горизонтальную прокрутку (с указанием виновных элементов), контент
под липкой панелью, разорванные суммы, модалку записи, ошибки JS.
Нужен `pip install playwright && playwright install chromium`; если в
окружении чужой билд — путь к бинарю через `PW_CHROMIUM=...`.

⚠️ Шрифты Google при проверке НЕ блокировать: без них подставляются
системные с другой шириной глифов и сыплются ложные «переполнения».

## Липкая панель «Позвонить/Записаться» и низ страницы

`.sticky-cta` — `position: fixed; bottom: 0`, показывается на ≤640px и
места в потоке не занимает. Под неё **зарезервирован отступ у футера**:
`padding-bottom: calc(30px + var(--sticky-cta-h) + env(safe-area-inset-bottom))`.
Без него последняя строка футера (копирайт и бейдж «Сделано в FutureFlow»)
уходит под панель — так и было до 31.07.2026.

Отступ именно на `.footer`, а не на `body`: тогда под полупрозрачной
панелью продолжается тёмный фон футера, а не светлая полоса.
`--sticky-cta-h: 66px` в `:root` — единый источник и для отступа футера,
и для позиции FAB. Меняешь паддинги `.sticky-cta` — правь переменную.

## Изображения

- **Реальные фото клиники** — `assets/img/clinic/`: `clinic-hero.webp`
  1000×1200 (hero главной), `clinic-about.webp` 1000×1150 (блоки «о нас»),
  `clinic-1..6.webp` 1280×960 (галерея «Интерьер» на главной),
  `og-banner.webp` 1200×630 (OG + JSON-LD). Исходники — папка Drive
  `1q77x0l2jd7PzNyGXqD6YiDTutUxuaTxf`.
- **Сгенерированные** — только `assets/img/services/*.webp` (1200×750,
  Higgsfield `soul_2`, 16:9 → кроп). Промпт-стиль: см. BRAND.md. Люди —
  сгенерированные, БЕЗ имён, славянская внешность.

## Врачи и портфолио (добавлено 2026-07-28)

Раздел по образцу Versal: `doctors/index.html` + страницы врачей
(hero → образование/сертификаты (`<!-- TODO -->`) → «Работы врача»
(`data-portfolio` из `assets/js/portfolio.js`) → перелинковка → CTA).
На главной — секция `#doctors` (грид из 3 карточек, перед «Акциями»).

**Фото врачей — единый стиль Венеции**: силуэт (вырезан из версальских
портретов difference-matting'ом по известному градиенту) на градиенте
**#DBEAE5 → #F4F9F7** (светлая лагуна → алебастр), кадр 4:5, арочная
маска в карточках. Файлы: `assets/img/doctors/<slug>.png|.webp`
(480×600) + `<slug>-thumb.png|.webp` (256×320). Киласония — пока
`placeholder.svg` (в цветах Венеции).

**Портфолио**: `assets/js/portfolio.js` (данные + рендер, лайтбокс
создаётся из JS), стили `.pf-*`/`.doctor-*` в конце `styles.css`.
Фото: оригинал 4:3 1200×896 (обезличено) → `_originals/` →
`python3 scripts/apply-watermark.py <имя>.webp`.

⚠️ **Водяной знак Венеции слабее, чем у Angel/Versal** (решение владельца,
28.07.2026): **0.32 короткой стороны кадра, прозрачность 0.62**, halo 0.38 —
против 0.4286/0.78 у тех сайтов. Причина: знак Венеции — плотная
плашка-логотип, а не лёгкая линейная графика, и на клиническом фото
перетягивал внимание. Ниже 0.26 зуб перестаёт читаться — не опускать.

⚠️ Размер считается от **короткой стороны**, а не от ширины. Знак
квадратный: при расчёте от ширины на широких снимках (`*-full`, например
1100×504) он занимал больше половины высоты кадра и в лайтбоксе выглядел
огромным, хотя формально это были «те же 24%». Параметры — в шапке
`scripts/apply-watermark.py`; меняешь их → перегенерировать ВСЕ фото
(запуск скрипта без аргументов, он идемпотентен — читает из `_originals/`).

**Общие врачи с Angel/Versal** (drobkova, kilasoniya, kendabaeva):
кейсы = те же кадры `_originals/` и тексты, НО в текстах Венеции
**вычищены КТ/КЛКТ** (заменены на ТРГ/ОПТГ/«рентгенодиагностика» —
КТ в клинике нет) **и марки сканеров** (3Shape → «цифровое
сканирование»). При синхронизации новых кейсов с других сайтов —
повторять эту адаптацию. Цитаты врачей тоже без КТ.

## Истории Яндекс.Бизнеса (сторис) — линейка v1 готова

Процесс и правила — общие с Angel/Versal (их CLAUDE.md, «Истории для
ЯБ»): 1080×1920, фоны Higgsfield (9:16), русский текст только PIL,
люди сгенерированные без имён, до/после нельзя, дисклеймер при ценах,
нижние ~230px свободны под кнопку Яндекса.

Линейка v1 (03.08.2026, 18 слайдов, БЕЗ истории про врачей — решение
владельца): 1 «Не страшно» → 2 «Цены честно» → 3 «Для всей семьи»
(семейная программа 3–10%) → 4 «Ровные зубы» (оффер: консультация
ортодонта 0 ₽) → 5 «Брекеты 30+» (миф про возраст) → 6 «Имплантация»
(Osstem от 28 000, 3-й имплант в подарок). В сюжете «Цены честно» НЕ
выделять одну услугу (была гигиена — заменена на «смету на все
услуги»): прайс широкий, одна услуга выглядит случайной. Реестр слайдов/ссылок/настроек —
`_materials/yb-stories/REGISTRY.md`; сборка — `venecia-stories.py`
(фоны в `bg/`, правишь текст → перегенерация без новых генераций).
Папка Drive: «Венеция — Истории ЯБ v1» (id `1TxoepERg5fIhkx8u1TwXVXxVrD5J_Sk9`),
там google-doc с порядком заливки. Свежая история показывается первой →
самый сильный оффер заливать последним.

## Паспорт имплантов (печать)

`_materials/implant-passport/`: `build-passport.py` собирает
двусторонний A4-паспорт с тройным фальцем (структура = Angel-Dent,
стиль Венеции). Гарантия — «по договору» без сроков; лицензия/юрлицо
НЕ указаны (реквизитов ещё нет — появятся, добавить строку).

⚠️ **Схема фальцовки — от типографии, панели НЕ равные:**
99,5 + 99,5 + 98 мм, сгибы на 99,5 и 199,0 мм от верхнего реза
(нижний клапан короче — складывается внутрь). Наружная сторона:
ЛИЦО (верх, вверх ногами) / ОБОРОТ (середина, прямо) / клапан (низ,
вверх ногами). Внутренняя — три панели прямо, секции строго по
панелям (в скрипте assert'ы на сгибы). Эталонные схемы —
`_materials/implant-passport/samples/`, у владельца НЕ запрашивать.
Полные правила — `_materials/implant-passport/DESIGN.md`.

## Наружка: билборд и консольная вывеска-зуб

⚠️ **Фасад клиники ЧЁРНЫЙ** (владелец, 11.08.2026) — тёмные варианты
вывески на нём проваливаются. Для чёрного фасада брать светлые схемы
(алебастр), фирменную лагуну оставлять для светлых фасадов и печати.

`_materials/naruzhka/` (не деплоится):
- `billboard.py` — билборд 3×6 м, 2 варианта (семья / гигиена). Обязательны
  полоса «Имеются противопоказания…» ≥5 % площади и строка юрлица с
  лицензией ООО «АНГЕЛ-ДЕНТ».
- `tooth-sign.py` — **консольная вывеска-зуб** (панель-кронштейн), фигурный
  рез по контуру зуба из `logo.svg`, высота зуба 700 мм (ширина ~670).
  4 цветовые схемы в словаре `SCHEMES`: `alabaster`, `alabaster-edge`,
  `lagoon`, `terracotta`. На каждую — печатный PDF 1:1; контур реза
  (`venecia-tooth-sign-cutline.pdf/.svg`) ОДИН на все схемы.
  Скрипт сам подбирает кегль «ВЕНЕЦИЯ» так, чтобы буквы не подходили к
  линии реза ближе 20 мм (карта расстояний по маске зуба) — не отключать.
- `tooth-sign-variants.py` — сравнение схем на чёрном фасаде, день+ночь.
- Вывеска = обязательная информация об организации, дисклеймер о
  противопоказаниях и лицензия на ней НЕ нужны (в отличие от билборда).

## Рекламные буклеты (печать) — цены ОТДЕЛЬНЫЕ от сайта

⚠️ В печатных буклетах свои акционные цены, действуют **только при
предъявлении буклета** и МОГУТ ОТЛИЧАТЬСЯ от сайта — это осознанное
решение владельца (02.08.2026), НЕ «синхронизировать» их с сайтом и не
переносить на сайт без явной просьбы. Состав акций буклета v1:
каждый 3-й имплант в подарок · скидка 10% многодетным, военнослужащим,
пенсионерам · комплексная гигиена 5 000 ₽ · брекеты 50 000 ₽ ·
имплант + циркониевая коронка 42 000 ₽.
Материалы и сборка — `_materials/buklet/` (build-booklet.py, не
деплоится). Стиль — фирменный Венеции (лагуна/терракота/алебастр,
Prata + Onest, арки), фото — Higgsfield soul_2, русский текст только
PIL. На каждой стороне: «Акция действует при предъявлении буклета»,
дисклеймер «Имеются противопоказания…» и «Не является публичной офертой».

## Правила текстов (важно)

- **Evergreen**: НЕ писать «мы открылись/открываемся», «новая клиника»,
  «до открытия», «третья клиника» — тексты не должны требовать правок
  после запуска (требование владельца, 2026-07-21).
- **Модалка записи** — только имя + телефон (без выбора услуги, решение
  владельца). Селект услуги остаётся в больших формах на index/contacts.
- В футере каждой страницы — бейдж «Сделано в FutureFlow» (`.ff-credit`).

## Акции в прайс-таблицах — единый формат (как у Angel/Versal)

```html
<tr><td>Название <span class="badge-promo">Акция</span></td>
    <td class="price-now">17 500 ₽ <s style="font-weight:400;opacity:.55">25 000 ₽</s></td></tr>
```
Сейчас так оформлены: Amazing White (otbelivanie), бесплатная
консультация ортодонта (ortodontiya, ceny.html).

Группы внутри прайс-таблицы — строка-подзаголовок
`<tr class="prices-subhead"><td colspan="2">Название группы</td></tr>`
(стиль в styles.css). Внутри группы услуги сортируем по цене по
возрастанию. Эталон — таблица гигиены (`services/gigiena.html`):
группы «Профессиональная чистка» / «Фторирование и укрепление эмали»
(+ «Отбеливание» перед акционной строкой Amazing White).

## Форма заявки и цели Метрики

Как у Angel/Versal: `main.js` (`sendLead`) → POST `/api/lead.php` →
Telegram. Honeypot `company`, маска `+7 (XXX) XXX-XX-XX`. Цели
(`trackGoal`): lead_submit, call_click, whatsapp_click, telegram_click —
макро; modal_open, form_start — микро. Имена целей в кабинете Метрики
должны совпадать. UTM/yclid — sessionStorage → `_utm_*` в заявке.

## Деплой на reg.ru

✅ **Сайт развёрнут и работает с 29.07.2026** — https://venecia-dent.ru/
отдаётся по https, всё проверено (страницы 200, редиректы, gzip).

С сервера (Shell-клиент ISPmanager): `~/venecia-dent.sh` = `git pull` +
rsync репо → `~/www/venecia-dent.ru/`. На сервере ТРИ клиники (Angel,
Versal, Венеция) — у каждой свой репо/папка/ярлык, не перепутать.
Не деплоятся: .git, scripts/, CLAUDE.md, README.md, BRAND.md,
_materials/, api/config.php.example, `assets/img/portfolio/_originals/`.

Что уже сделано на сервере (повторять не надо):
- репо склонирован в `~/venecia-dent.ru/`, **ветка `main`**;
- `api/config.php` создан из `.example` — **пока с заглушкой вместо
  токена**, заявки в Telegram не уходят (см. TODO);
- симлинк `~/venecia-dent.sh → ~/venecia-dent.ru/scripts/deploy.sh`;
- публичная папка `~/www/venecia-dent.ru/`.

**DNS/хостинг:** домен на NS `ns1/ns2.hosting.reg.ru`, A-записи `@` и
`www` → `31.31.197.39` (тот же сервер, что у Angel/Versal). При
подключении домена делегирование в зоне `.RU` обновлялось ~1 час — всё
это время домен резолвился на парковку `95.163.244.138`, и выпуск
Let's Encrypt падал с 404 на `/.well-known/acme-challenge/`. Если
повторится с другим доменом: проверять `dig NS <домен>
@a.dns.ripn.net +noall +authority` (истинное делегирование, без кешей)
и `dig +short A <домен> @1.1.1.1`; `whois` показывает новые NS раньше,
чем зона `.RU` их публикует. Ждать, не тратя попытки LE (лимит —
5 неудачных проверок на домен в час).

**SSL:** сертификат Let's Encrypt (`venecia-dent.ru_le1`) на
`venecia-dent.ru` + `www`, выпускается в ISPmanager → SSL-сертификаты.
Редирект на https и склейка www — **в `.htaccess` репозитория**
(как у Angel/Versal), галочку редиректа в панели не включать.
⚠️ `.htaccess` гонит на https безусловно — до выпуска сертификата
на новом домене его деплоить нельзя.

**Автодеплой через GitHub Actions (настроено 10.08.2026):** пуш в `main`
или в `claude/venetia-dental-website-vr6cb6` запускает
`.github/workflows/deploy.yml` — раннер GitHub по SSH выполняет
`~/venecia-dent.sh` на сервере. Пароль SSH — в секрете репозитория
**`REGRU_SSH_PASSWORD`** (в git НЕ хранить). Из CCR-контейнера прямого
SSH нет (наружу открыт только 443). Произвольная команда на сервере —
workflow `server-command.yml` в репо Angel (сервер общий для трёх клиник).

## Workflow пушей

Разработка — на сессионной ветке (см. инструкции сессии), пуш
`git push -u origin <ветка>`.

⚠️ **ПРАВИЛО ВЫКАТКИ (владелец, 10.08.2026): пуш в `main` ИЛИ в дефолтную
`claude/venetia-dental-website-vr6cb6` = автодеплой на прод через GitHub
Actions. В эти ветки пушим ТОЛЬКО после явного подтверждения владельца
(«выкатывай»): сделал правки → показал → дождался «ок» → тогда пуш
фастфорвардом в обе (`main` и дефолтную — держать их в синхроне).**
Во время работы — только сессионная ветка. PR не создавать.

## Что НЕ делать без явной просьбы

- Не выдумывать отзывы/рейтинги/врачей/реквизиты.
- Не упоминать КТ/томографию и детскую стоматологию — в клинике их НЕТ
  (владелец, 2026-07-24). Не упоминать прочее конкретное оборудование
  (микроскоп, марки сканеров), пока владелец не подтвердил оснащение.
  ТРГ+ОПТГ в прайсе оставлены — при подтверждении оснащения проверить.
- Не писать слово «премиум» в текстах сайта.
- Не пушить только в фичевую ветку — забыть про `main` и дефолтную
  `claude/venetia-dental-website-vr6cb6` = правка не доедет до прода
  (см. «Workflow пушей»).
